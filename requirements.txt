# Core
openai-whisper>=20231117
flask>=3.0
python-dotenv>=1.0
yt-dlp>=2024.0

# Production servers
gunicorn>=22.0         # Linux/Mac/Docker production server
waitress>=3.0          # Windows-compatible production server (local dev fallback)

# YouTube Upload
google-auth>=2.20
google-auth-oauthlib>=1.0
google-api-python-client>=2.90

# AI Commentary / TTS
edge-tts>=6.1          # Microsoft Edge neural TTS (free, no API key)

# Content Factory — visual generation
Pillow>=10.0           # Text rendering for video scene generation
requests>=2.31         # API calls (stock footage, trends, etc.)

# LLM Fallback (optional — install only what you need)
groq>=0.9
google-generativeai>=0.7
openai>=1.30           # for NVIDIA NIM (OpenAI-compatible API)
